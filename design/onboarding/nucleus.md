# the taste nucleus — visual spec (founder-locked)

The connective organism across THE FITTING (dark/inverted screens; the feed stays paper — the
flip to light IS the payoff of `ENTER YOUR FEED →`).

HARD RULE (Charles): **the nucleus is NEVER an empty circle.** It opens lightly dense — a visible,
living particle form from the very first frame (density floor ~0.15, softly drifting) — and
onboarding is the experience of watching it become fully formed. Growth, never birth-from-nothing.

- particles accrue per SIGNAL (swipes + archive uploads both count — SIGNALS n/n is total fed)
- cluster arcs seed from the user's actual fitted weights (two opposite users -> visibly
  different nuclei); deterministic per user
- DRIFT: WANDERING -> SETTLING -> STABLE = real weight-convergence between swipe batches
- NUCLEUS COMPOSITION chips = top positive fitted attribute weights, verbalized
- states: cover (light-density idle) -> deck corner mini (accruing) -> milestone zoom-ins ->
  the Read (nucleus + composition = the reveal) -> build finale (full density animation,
  PROFILE FORMED stamp) -> flash to paper feed
- no step counters anywhere (audit rule); `PROFILE FORMED` label, not `03/03`
- one canvas, ~2-4k particles, rAF; no DOM churn, no blur (iOS-safe)
